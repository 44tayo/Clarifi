import React from 'react'
import ReactDOM from 'react-dom/client'

import { DictationPill } from './dictation-pill'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DictationPill />
  </React.StrictMode>,
)
